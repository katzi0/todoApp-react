{
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mongodb": "^3.1.10",
    "nodemon": "^1.18.9"
  },
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon index.js --exec babel-node --presets es2015",
    "build": "yarn run test && npm run clean:dist && babel src -d dist"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1"
  }
}
